//reverse and join with arrays
let letters = ['t', 'c', 'e', 'p', 's', 'e', 'R'];

letters.reverse()
//letters.join()
//letters.join('.')
//letters.join('-')
//[12,3,600,flase,true].join() will still work and convert into new strings

